"""
This is the __init__.py module makes the directory 'packages' a package.
The directory contains several modules that through __init__ are considered by 
the Python interpreter as part of one package only: "packages".

__init__.py might contain initialization statements. It does not contain any in this case. 
"""
